# img/

Empty for now. Referenced/expected assets:

- `og-cover.jpg` — social share preview image, referenced in `index.html`'s
  `<meta property="og:image">` tag (800x800 recommended, matching the old site).
- Anything else you want to add — logos for client engagements, screenshots
  for the Dashboards section, etc. None of that is wired up yet since no
  theme/brand PDF has been received.
